name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Versioni aggiornate
  http: ^1.5.0
  google_sign_in: ^7.2.0
  web: ^1.1.1
  
  # Le altre dipendenze rimangono invariate
  provider: ^6.1.2
  flutter_secure_storage: ^9.0.0
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Versione aggiornata
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/google_logo.png